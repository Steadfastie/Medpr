@model FamilyMemberModel

@{
    ViewData["Title"] = "Family tie details";
}

<table class="table table-borderless">
    <tbody>
        <tr>
            <td>
                @{await Html.RenderPartialAsync("FamilyMemberPartial", Model);}
            </td>
            <td>
                <a class="btn btn-primary" href="~/familyMembers/edit/@Model.Id">Edit</a>
                <a class="btn btn-danger" href="~/familyMembers/delete/@Model.Id">Delete</a>
            </td>
        </tr>
    </tbody>
</table>

@*This page will contain:
    CRUD buttons
    additional information like drug description paper or wikiAPI*@
