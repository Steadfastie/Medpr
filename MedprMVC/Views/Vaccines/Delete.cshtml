@model VaccineModel

@{
    ViewData["Title"] = "Delete vaccine";
}

<table class="table table-borderless">
    <tbody>
        <tr>
            <td>
                @{
                    await Html.RenderPartialAsync("VaccineDetailsPreview", Model);
                }
            </td>
            <td>
                <form asp-controller="Vaccines" asp-action="Delete" method="post">
                    <input type="submit" name="@Model.Id" class="btn btn-primary" value="Yes">
                </form>
            </td>
            <td>
                <a class="btn btn-primary" href="~/vaccines/details/@Model.Id">No</a>
            </td>
        </tr>
    </tbody>
</table>