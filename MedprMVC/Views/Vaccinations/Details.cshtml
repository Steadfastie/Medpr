@model VaccinationModel

@{
    ViewData["Title"] = "Vaccination details";
}

<table class="table table-borderless">
    <tbody>
        <tr>
            <td>
                @{await Html.RenderPartialAsync("VaccinationPartial", Model);}
            </td>
            <td>
                <a class="btn btn-primary" href="~/vaccinations/edit/@Model.Id">Edit</a>
                <a class="btn btn-danger" href="~/vaccinations/delete/@Model.Id">Delete</a>
            </td>
        </tr>
    </tbody>
</table>

@*This page will contain:
    CRUD buttons
    additional information like drug description paper or wikiAPI*@
